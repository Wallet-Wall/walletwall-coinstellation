{
  "name": "@walletwall/coinstellation",
  "version": "1.0.0",
  "description": "Coinstellation — read-only wallet and entity relationship graph. Part of the WalletWall analytics suite.",
  "private": false,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "security:audit": "npm audit --audit-level=moderate",
    "check": "npm run test && npm run build && npm run security:audit"
  },
  "dependencies": {
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.3.3",
    "@testing-library/user-event": "^14.6.6",
    "@vitejs/plugin-react": "^6.1.1",
    "jsdom": "^29.1.1",
    "vite": "^8.2.2",
    "vitest": "^3.2.4"
  }
}
